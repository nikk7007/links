# Bloqueia acesso direto a arquivos sensíveis da API.
# (config.php já é executado como PHP e não vaza o código-fonte, mas reforçamos.)
<FilesMatch "(config(\.sample)?\.php|make-hash\.php|\.sql|SETUP\.md)$">
  Require all denied
</FilesMatch>
